# esp32-gps-tracker
Using an ESP32, a NEO-6M (gps module) and an A7670E (LTE 4g module), I built a GPS traker that sends its position (using UDP protocol) every hour.

All the code was generated using Claude (Sonnet 4.6).

No electronic scheme, figure it yourself.
